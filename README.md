# OSS take notes 📝
## Git Bash를 실행하고 주요 6가지 설정 🔧
```bash

# 사용자 이름
＄git config --global user.name jh04327

# 사용자 전자메일
＄git config --global user.email kim47134@gmail.com

# 자동 줄바꿈
＄git config --global core.autocrlf true

# 안전 줄바꿈
＄git config --global core.safecrlf false

# 기본 편집기 설정
＄git config --global core.ebitor 'code --wait'

# 기본 브랜치 이름 설정
＄git config --global init.defaultBranch main

# 저장소 생성
＄git init basic

# 폴더 이동
＄cd basic

# 파일 확인
＄ls -al
